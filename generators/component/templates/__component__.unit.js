import {
  shallow
} from 'vue-test-utils';

import component from './<%= props.componentName %>';

describe('unit: <%= props.componentName %>', () => {

  describe('data', () => {

    // eslint-disable-next-line
    const wrapper = shallow(component);

  });

  describe('methods', () => {

    // eslint-disable-next-line
    const wrapper = shallow(component);

  });

});
